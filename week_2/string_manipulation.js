/*
You are given a string `weaponLoadout` containing weapon names (all lowercase) 
and a target weapon `targetWeapon`.  

Your task is to implement a function `findTargetIndex(weaponLoadout, targetWeapon)`  
that returns the starting index of the **first occurrence** of `targetWeapon` in `weaponLoadout`.  

If `targetWeapon` is not found in `weaponLoadout`, the function should return -1.

Examples (Weapons Edition):
console.log(findTargetIndex("lancergnasherboomshot", "gnasher"));  
// Output: 6

console.log(findTargetIndex("torquebowretrohammerburst", "boltok"));  
// Output: -1

console.log(findTargetIndex("longshotscorcherboltok", "scorcher"));  
// Output: 8

console.log(findTargetIndex("lancerboomshotmulcher", "mulcher"));  
// Output: 12
*/
